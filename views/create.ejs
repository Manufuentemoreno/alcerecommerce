<!DOCTYPE html>
<html lang="es">
  <head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-0evHe/X+R7YkIZDRvuzKMRqM+OrBnVFBL6DOitfPri4tjfHxaWutUpFmBp4vmVor" crossorigin="anonymous">
    <%- include("partials/head")  %> 
    <title>Productos</title>
  </head>
  <body>
    <header>
      <%- include("partials/topBar")  %> 
    </header>
    <main>
      <section class="box">
        <h1>Producto Nuevo</h1>

        <form class="form" action="/product/crear" method="POST" enctype="multipart/form-data">
            
            <input class="input-stle <%= locals.errors && errors.productName ? "danger" : null %>"
            type="text" name="productName" id="productName" placeholder="Nombre del Producto"
            value= <%= locals.oldData ? oldData.productName : null %>>
        <% if (locals.errors && errors.productName) { %>
            <div class="danger-text">
            <p><%= errors.productName.msg %></p>
            </div> 
        <% } %>

            <input style="-moz-appearance: textfield;-webkit-outer-spin-button: none; -webkit-inner-spin-button: none"
             class="input-stle <%= locals.errors && errors.price ? "danger" : null %>" type="number" name="price" id="price" placeholder="Precio"
             value= <%= locals.oldData ? oldData.price : null %>>
            <% if (locals.errors && errors.price) { %>
                <div class="danger-text">
                <p><%= errors.price.msg %></p>
                </div> 
            <% } %>

            <select class="input-stle" name="category" id="category">
                <option value="" disabled selected>Elegí una Categoría</option>
                <option value="Bebidas">Bebidas</option>
                <option value="Golosinas">Golosinas</option>
                <option value="Limpieza">Limpieza</option>
                <option value="Lacteos">Lacteos</option>
                <option value="Carnicería">Carnicería</option>
            </select>
            <% if (locals.errors && errors.category) { %>
                <div class="danger-text">
                <p><%= errors.category.msg %></p>
                </div> 
            <% } %>

            <div style="margin:10px; padding:10px;">
            <label for="preciosCuidados">Este producto se encuentra en Precios Cuidados</label>
            <input type="checkbox" name="preciosCuidados" id="preciosCuidados">
            </div>
            
            <textarea class="input-stle <%= locals.errors && errors.description ? "danger" : null %>" type="text" name="description" id="description" placeholder="Descripción"cols="30" rows="10"
                ><%= locals.oldData ? oldData.description : null %></textarea>
            <% if (locals.errors && errors.description) { %>
                <div class="danger-text">
                <p><%= errors.description.msg %></p>
                </div> 
            <% } %>

            <div style="margin:10px; padding:10px;">
            <label for="img" class="form-label">Imagen del Producto: </label>
            <input type="file"
                    id="img"
                    name="img"
                    style="margin-left: 20px"
            />
            <% if (locals.errors && errors.img) { %>
                <div class="danger-text">
                <p><%= errors.img.msg %></p>
                </div> 
            <% } else if (locals.errors) { %>
                <div class="danger-text">
                    <p>Por Favor, volvé a seleccionar la imagen</p>
                    </div> 
            <% } %> 
        </div>
        <div>
        <button 
        type="submit">Cargar Producto</button>
        <button type="reset">Borrar Todo</button>
    </div></form>
      </section>  
    </main>
    <%- include("partials/footer")  %> 
  </body>
</html>